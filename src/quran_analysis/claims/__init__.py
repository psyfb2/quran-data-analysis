"""Claims layer — schema, register loading, check registry and claim-runner.

Placeholder package — implemented across Tasks 5/8/9. The claims register
(claims.yaml) is data governed by a machine-readable schema; each claim's check
is a small callable registered to a registry so the runner stays open/closed.
"""
